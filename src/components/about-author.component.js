import React, {Component} from "react"

export default class AboutAuthor extends Component {
    render(){
        return(
            <span>
                Author is coool!
            </span>
        )
    }
}